/* empty css                                */
import { e as createComponent, m as maybeRenderHead, g as addAttribute, r as renderTemplate, h as createAstro, n as defineScriptVars, k as renderComponent } from '../chunks/astro/server_zKUY4SJo.mjs';
import 'piccolore';
import { $ as $$Layout } from '../chunks/Layout_Csh5F8eE.mjs';
import { $ as $$GeoTag } from '../chunks/GeoTag_Baoe_JH4.mjs';
import { $ as $$TrafficTag } from '../chunks/TrafficTag_DOXxKguS.mjs';
import 'clsx';
import { supabase } from '../chunks/supabase_CUAAmmKv.mjs';
export { renderers } from '../renderers.mjs';

const $$Astro = createAstro();
const $$RatingBadge = createComponent(($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro, $$props, $$slots);
  Astro2.self = $$RatingBadge;
  const { rating, size = "sm" } = Astro2.props;
  if (!rating) return;
  const color = rating >= 8 ? "text-success border-success/40 bg-success/10" : rating >= 5 ? "text-warning border-warning/40 bg-warning/10" : "text-danger border-danger/40 bg-danger/10";
  const dots = Array.from({ length: 10 }, (_, i) => i + 1);
  return renderTemplate`${rating != null && renderTemplate`${maybeRenderHead()}<div class="flex items-center gap-1.5"><div class="flex gap-0.5">${dots.map((i) => renderTemplate`<span${addAttribute(`rounded-full ${size === "sm" ? "w-1.5 h-1.5" : "w-2 h-2"} ${i <= rating ? rating >= 8 ? "bg-success" : rating >= 5 ? "bg-warning" : "bg-danger" : "bg-gray-700"}`, "class")}></span>`)}</div><span${addAttribute(`text-xs font-bold border rounded px-1 py-0.5 ${color}`, "class")}>${rating}/10</span></div>`}`;
}, "/Users/macbookpro15/Desktop/GitHub Adv/ADvatnage BizDev/src/components/RatingBadge.astro", void 0);

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Index = createComponent(async ($$result, $$props, $$slots) => {
  const { data: buyers } = await supabase.from("buyers").select("*").order("created_at", { ascending: false });
  const buyersJson = JSON.stringify(buyers ?? []);
  return renderTemplate(_a || (_a = __template(["", " <script>(function(){", "\n  const buyers = JSON.parse(buyersJson);\n  const cards = document.querySelectorAll('.buyer-card');\n  const emptyFilter = document.getElementById('empty-filter');\n  const countEl = document.getElementById('count');\n\n  function applyFilters() {\n    const search = document.getElementById('search').value.toLowerCase();\n    const geo = document.getElementById('filter-geo').value.toLowerCase();\n    const traffic = document.getElementById('filter-traffic').value.toLowerCase();\n    const ratingMin = Number(document.getElementById('filter-rating').value) || 0;\n\n    let visible = 0;\n    cards.forEach(card => {\n      const name = card.dataset.name ?? '';\n      const cardGeo = card.dataset.geo ?? '';\n      const cardTraffic = card.dataset.traffic ?? '';\n      const cardRating = Number(card.dataset.rating) || 0;\n\n      const ok =\n        (!search || name.includes(search)) &&\n        (!geo || cardGeo.includes(geo)) &&\n        (!traffic || cardTraffic.includes(traffic)) &&\n        (!ratingMin || cardRating >= ratingMin);\n\n      card.style.display = ok ? '' : 'none';\n      if (ok) visible++;\n    });\n\n    countEl.textContent = `${visible} of ${cards.length}`;\n    emptyFilter.classList.toggle('hidden', visible > 0);\n  }\n\n  ['search', 'filter-geo', 'filter-traffic', 'filter-rating'].forEach(id => {\n    document.getElementById(id)?.addEventListener('input', applyFilters);\n  });\n\n  applyFilters();\n\n  // CSV export\n  document.getElementById('export-csv')?.addEventListener('click', () => {\n    const headers = ['Name','Contact','Email','Telegram','GEOs','Traffic Types','Budget','Rating','Notes'];\n    const rows = buyers.map(b => [\n      b.name,\n      b.contact_name ?? '',\n      b.contact_email ?? '',\n      b.contact_telegram ?? '',\n      (b.geo_expertise ?? []).join(';'),\n      (b.traffic_types ?? []).join(';'),\n      b.monthly_budget_capacity ?? '',\n      b.rating ?? '',\n      (b.notes ?? '').replace(/\\n/g, ' '),\n    ]);\n    const csv = [headers, ...rows].map(r => r.map(v => `\"${String(v).replace(/\"/g, '\"\"')}\"`).join(',')).join('\\n');\n    const blob = new Blob([csv], { type: 'text/csv' });\n    const a = document.createElement('a');\n    a.href = URL.createObjectURL(blob);\n    a.download = 'buyers.csv';\n    a.click();\n  });\n})();<\/script>"], ["", " <script>(function(){", "\n  const buyers = JSON.parse(buyersJson);\n  const cards = document.querySelectorAll('.buyer-card');\n  const emptyFilter = document.getElementById('empty-filter');\n  const countEl = document.getElementById('count');\n\n  function applyFilters() {\n    const search = document.getElementById('search').value.toLowerCase();\n    const geo = document.getElementById('filter-geo').value.toLowerCase();\n    const traffic = document.getElementById('filter-traffic').value.toLowerCase();\n    const ratingMin = Number(document.getElementById('filter-rating').value) || 0;\n\n    let visible = 0;\n    cards.forEach(card => {\n      const name = card.dataset.name ?? '';\n      const cardGeo = card.dataset.geo ?? '';\n      const cardTraffic = card.dataset.traffic ?? '';\n      const cardRating = Number(card.dataset.rating) || 0;\n\n      const ok =\n        (!search || name.includes(search)) &&\n        (!geo || cardGeo.includes(geo)) &&\n        (!traffic || cardTraffic.includes(traffic)) &&\n        (!ratingMin || cardRating >= ratingMin);\n\n      card.style.display = ok ? '' : 'none';\n      if (ok) visible++;\n    });\n\n    countEl.textContent = \\`\\${visible} of \\${cards.length}\\`;\n    emptyFilter.classList.toggle('hidden', visible > 0);\n  }\n\n  ['search', 'filter-geo', 'filter-traffic', 'filter-rating'].forEach(id => {\n    document.getElementById(id)?.addEventListener('input', applyFilters);\n  });\n\n  applyFilters();\n\n  // CSV export\n  document.getElementById('export-csv')?.addEventListener('click', () => {\n    const headers = ['Name','Contact','Email','Telegram','GEOs','Traffic Types','Budget','Rating','Notes'];\n    const rows = buyers.map(b => [\n      b.name,\n      b.contact_name ?? '',\n      b.contact_email ?? '',\n      b.contact_telegram ?? '',\n      (b.geo_expertise ?? []).join(';'),\n      (b.traffic_types ?? []).join(';'),\n      b.monthly_budget_capacity ?? '',\n      b.rating ?? '',\n      (b.notes ?? '').replace(/\\\\n/g, ' '),\n    ]);\n    const csv = [headers, ...rows].map(r => r.map(v => \\`\"\\${String(v).replace(/\"/g, '\"\"')}\"\\`).join(',')).join('\\\\n');\n    const blob = new Blob([csv], { type: 'text/csv' });\n    const a = document.createElement('a');\n    a.href = URL.createObjectURL(blob);\n    a.download = 'buyers.csv';\n    a.click();\n  });\n})();<\/script>"])), renderComponent($$result, "Layout", $$Layout, { "title": "Buyers" }, { "default": async ($$result2) => renderTemplate` ${maybeRenderHead()}<div class="flex items-center justify-between mb-6"> <div> <h1 class="text-2xl font-bold">Buyers</h1> <p class="text-gray-500 text-sm mt-1">Media buyers in your network</p> </div> <div class="flex items-center gap-3"> <button id="export-csv" class="text-sm text-gray-400 hover:text-gray-200 border border-border px-4 py-2 rounded-lg bg-card-2 transition-colors">
↓ Export CSV
</button> <a href="/buyers/add" class="bg-accent hover:bg-accent-dark text-white text-sm font-medium px-5 py-2.5 rounded-lg transition-colors shadow-lg shadow-accent/20">
+ Add Buyer
</a> </div> </div>  <div class="flex flex-wrap gap-3 mb-6"> <input id="search" type="text" placeholder="Search by name or contact…" class="flex-1 min-w-48 bg-card border border-border rounded-lg px-4 py-2 text-sm text-gray-200 placeholder-gray-600 focus:border-accent outline-none"> <input id="filter-geo" type="text" placeholder="Filter by GEO (e.g. DE)" class="w-40 bg-card border border-border rounded-lg px-4 py-2 text-sm text-gray-200 placeholder-gray-600 focus:border-accent outline-none"> <select id="filter-traffic" class="bg-card border border-border rounded-lg px-4 py-2 text-sm text-gray-300 focus:border-accent outline-none"> <option value="">All traffic types</option> <option>Meta Ads</option> <option>InApp</option> <option>DSP</option> <option>Google Ads</option> <option>TikTok Ads</option> </select> <select id="filter-rating" class="bg-card border border-border rounded-lg px-4 py-2 text-sm text-gray-300 focus:border-accent outline-none"> <option value="">Any rating</option> <option value="8">8+ (Top)</option> <option value="5">5+ (Good)</option> <option value="1">Rated</option> </select> <span id="count" class="self-center text-xs text-gray-600 font-mono"></span> </div> ${!buyers || buyers.length === 0 ? renderTemplate`<div class="bg-card rounded-xl border border-border p-12 text-center"> <p class="text-gray-500">No buyers yet.</p> <a href="/buyers/add" class="text-accent hover:underline text-sm mt-2 inline-block">Add your first buyer</a> </div>` : renderTemplate`<div id="grid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4"> ${buyers.map((buyer) => renderTemplate`<a${addAttribute(`/buyers/${buyer.id}`, "href")} class="buyer-card bg-card rounded-xl border border-border p-6 hover:border-accent/40 transition-all group block relative overflow-hidden"${addAttribute(`${buyer.name} ${buyer.contact_name ?? ""}`.toLowerCase(), "data-name")}${addAttribute((buyer.geo_expertise ?? []).join(" ").toLowerCase(), "data-geo")}${addAttribute((buyer.traffic_types ?? []).join(" ").toLowerCase(), "data-traffic")}${addAttribute(buyer.rating ?? "", "data-rating")}> <div class="absolute inset-0 bg-gradient-to-br from-accent/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity pointer-events-none"></div> <div class="flex items-start justify-between mb-3"> <div class="flex-1 min-w-0"> <h3 class="font-semibold group-hover:text-accent transition-colors truncate">${buyer.name}</h3> ${buyer.contact_name && renderTemplate`<p class="text-gray-500 text-xs mt-0.5">${buyer.contact_name}</p>`} </div> <span class="text-xs font-mono bg-card-2 border border-border px-2 py-1 rounded text-gray-400 ml-2 shrink-0">
$${Number(buyer.monthly_budget_capacity || 0).toLocaleString()} </span> </div> ${buyer.rating != null && renderTemplate`<div class="mb-3"> ${renderComponent($$result2, "RatingBadge", $$RatingBadge, { "rating": buyer.rating })} </div>`} <div class="flex flex-wrap gap-1 mb-2"> ${(buyer.geo_expertise ?? []).map((g) => renderTemplate`${renderComponent($$result2, "GeoTag", $$GeoTag, { "code": g })}`)} ${(buyer.geo_expertise ?? []).length === 0 && renderTemplate`<span class="text-gray-700 text-xs italic">No GEOs set</span>`} </div> <div class="flex flex-wrap gap-1"> ${(buyer.traffic_types ?? []).map((t) => renderTemplate`${renderComponent($$result2, "TrafficTag", $$TrafficTag, { "type": t })}`)} </div> </a>`)} </div>`}<div id="empty-filter" class="hidden bg-card rounded-xl border border-border p-12 text-center"> <p class="text-gray-500">No buyers match your filters.</p> </div> ` }), defineScriptVars({ buyersJson }));
}, "/Users/macbookpro15/Desktop/GitHub Adv/ADvatnage BizDev/src/pages/buyers/index.astro", void 0);

const $$file = "/Users/macbookpro15/Desktop/GitHub Adv/ADvatnage BizDev/src/pages/buyers/index.astro";
const $$url = "/buyers";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$Index,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
